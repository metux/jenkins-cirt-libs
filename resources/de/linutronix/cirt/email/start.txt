CI-RT Testjob ${BRANCH_NAME} #${BUILD_NUMBER} starts

It is based on test-description branch ${BRANCH_NAME}

Further test-description git information:
git url: ${GIT_URL}
commit: ${GIT_COMMIT}.

For console log of current testjob please look at ${BUILD_URL}console
